import { ThirdDirectiveDirective } from './third-directive.directive';

describe('ThirdDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ThirdDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
